{let_} = require \glad-functions

module.exports =
  # Type signature for all
  # ExpressApp -> ...
  listen: let_ _, \listen, _
  set: let_ _, \set, _, _
  use: let_ _, \use, _
  get: let_ _, \get, _, _
  post: let_ _, \post, _, _
  put: let_ _, \put, _, _
  delete: let_ _, \delete, _, _
